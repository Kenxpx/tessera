# Keep shell scripts LF so the shebang works in the Linux fuzzing image.
*.sh text eol=lf

# Binary fixtures must never be EOL-converted.
*.tsb binary

# Normalize source to LF.
*.c   text eol=lf
*.h   text eol=lf
*.py  text eol=lf
*.md  text eol=lf
*.txt text eol=lf
*.yaml text eol=lf
